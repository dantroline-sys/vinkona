{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The launcher window may invoke the app's own commands and the folder dialog.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
