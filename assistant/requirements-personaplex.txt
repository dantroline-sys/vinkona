# LEGACY: PersonaPlex/moshi dependencies — needed ONLY for the original
# speech-to-speech path (personaplex_server.py). The cascade does not use any
# of this; install_personaplex.sh installs it on top of requirements.txt.
#
# ⚠ Build reality check: sphn/rustymimi pin old pyo3, and prebuilt wheels stop
# at Python 3.13 — on a newer interpreter (e.g. Fedora's 3.14) pip falls back
# to a source build that FAILS (pyo3 max version; opus's ancient CMake floor).
# Use a Python ≤ 3.13 venv for this stack, or force your way through with:
#   PYO3_USE_ABI3_FORWARD_COMPATIBILITY=1 CMAKE_POLICY_VERSION_MINIMUM=3.5 \
#     pip install -r requirements-personaplex.txt

# Core PyTorch — pick the wheel index to match your driver, e.g.:
#   pip install -r requirements-personaplex.txt \
#       --extra-index-url https://download.pytorch.org/whl/cu124
# (install_personaplex.sh handles this automatically.)
torch

# NVIDIA PersonaPlex engine — install from source: pip install personaplex_src/moshi/.
# DO NOT pip install moshi from PyPI — it conflicts with moshi-personaplex.
# Version pins match moshi-personaplex 0.1.0 requirements.
rustymimi
sphn>=0.1.4,<0.2
sentencepiece
bitsandbytes
safetensors>=0.4.0,<0.5

# WebRTC + audio pipeline (the rust/WebRTC serving mode)
aiortc
av
